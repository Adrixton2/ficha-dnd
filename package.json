{
  "name": "ficha-dnd-pwa",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/pure-utils.test.mjs tests/service-worker-cache.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-ficha-dnd \"node --test tests/firestore.rules.test.mjs\"",
    "test:all": "npm test && npm run test:rules"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.2"
  },
  "overrides": {
    "@google-cloud/pubsub": "6.0.1",
    "uuid": "11.1.1"
  }
}
